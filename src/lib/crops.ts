/**
 * Canonical crop list — re-exported from wizard-enums so callers can import
 * from a stable, focused module path (`@/lib/crops`). See wizard-enums.ts for
 * the source of truth.
 */

export { CROP_OPTIONS } from './wizard-enums';
export type { EnumOption } from './wizard-enums';
